node_modules
# Keep environment variables out of version control
.env

/src/generated/prisma

# Runtime / local artifacts
dump.rdb
/uploads
.agents
skills-lock.json

# Load-test session tokens (minted by seed-load-test-users.ts) - live API credentials
loadtest/k6/pool.json
./claude
